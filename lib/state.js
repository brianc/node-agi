/*
 * --------------------------------------------------------------
 * State options for our contexts.
 * --------------------------------------------------------------
 */
var state = {
	init: 0,
	waiting: 2
};

module.exports = state;
